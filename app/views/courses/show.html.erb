<% provide :title, @course.name %>
<div class="row">
  <aside class="col-md-4">
    <section class="course_info">
      <h1>
        <%= @course.name %>
      </h1>
      <p><%= @course.description %></p>
    </section>
  </aside>
</div>
