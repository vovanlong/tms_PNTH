<%= form_for([:supervisor, load_user_form(@course, @user)],
  html: {method: :delete}, remote: true) do |f| %>
  <%= hidden_field_tag :user_id, @user.id %>
  <%= hidden_field_tag :course_id, @course.id %>

  <%= f.submit t(".btn_submit"), class: "btn btn-danger" %>
<% end %>
