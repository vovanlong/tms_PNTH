$('#new_user_course_<%= @user_course.user.id %>').parent().html('<%= j(render('supervisor/courses/button/remove_user', object: @user_course)) %>');
showAlert(I18n.t("alert.added"));

function showAlert(message){
  $('#alert').html(message);
  $('#alert').show().fadeIn().delay(3000).fadeOut();
}
